{
  "name": "@hvcd/server-worker",
  "private": true,
  "version": "0.1.0",
  "description": "HVCD ranked (T3) Cloudflare Worker — project-hosted server replay and arbitration endpoint per platform-capability-server-auth.md § T3.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --experimental-strip-types --no-warnings tests/arbitrate.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.8.3",
    "wrangler": "^3.104.0"
  }
}
